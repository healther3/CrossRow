# GCP 凭据配置
gcp:
  credentials:
    location: file:./config/gcp-key.json

spring:
  ai:
    vertex:
      ai:
        gemini:
          project-id: crossrow
          location: us-central1
          # 关键配置：指定凭证文件路径，解决 ADC 认证问题
          credentials-uri: file:./config/gcp-key.json
          chat:
            options:
              model: gemini-2.5-flash
              google-search-retrieval: true
        embedding:
          project-id: crossrow
          location: us-central1
          # Embedding 也需要凭证
          credentials-uri: file:./config/gcp-key.json
          text:
            options:
              model: text-embedding-005
  data:
    redis:
      host: localhost
      port: 6379
      password:
      database: 0
  application:
    name: CrossRow
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: dyx1326022894
  elasticsearch:
    host: localhost
    port: 9200
    protocol: http
    connection-timeout: 5000
    socket-timeout: 60000
    index-name: philosophy_docs

app:
  image-model:
    model: gemini-2.5-flash-image      # Nano Banana
    project-id: crossrow
    location: us-central1
    aspect-ratio: "16:9"
    image-size: "1K"
    save-path: "F:/AI-Agent/CrossRow/generated-images"
    api-key: "AIzaSyC_zmgihezprxblT9iQcNSvu8JWWL46t4M"
